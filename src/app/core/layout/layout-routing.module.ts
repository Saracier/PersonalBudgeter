import { Routes } from '@angular/router';

const routes: Routes = [
  {
    path: 'historia',
    data: { title: 'Historia' },
  },
];

// Nasłuchiwać na zmianę routingu / pobierać data z konkretnego route i przekazywać go do header.component
